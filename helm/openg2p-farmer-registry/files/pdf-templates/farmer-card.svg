<svg xmlns="http://www.w3.org/2000/svg" width="600" height="380" viewBox="0 0 600 380">
  <!--
    Farmer Registry credential card.

    This is the printed artefact a farmer carries, so it is a real design file,
    not markup buried in code: a designer edits it here and the Helm chart ships
    it as a ConfigMap. The Agent Portal API mounts it and substitutes the
    {{...}} placeholders at render time, then converts SVG -> PDF.

    Placeholders:
      {{title}}              card title            (chart: agentPortalApi.pdf.title)
      {{issuerName}}         issuing authority     (chart: agentPortalApi.pdf.issuerName)
      {{qr}}                 signed QR, data-URI   (generated)
      {{fullName}}           )
      {{functionalRecordId}} )  claims, from farmer_vc_view
      {{dateOfBirth}}        )
      {{gender}}             )
      {{photo}}              empty in Phase 1 — the photograph is deferred to
                             Phase 2, so the frame below stays blank for now
                             rather than the layout shifting when it arrives.
  -->
  <rect width="600" height="380" fill="#ffffff" stroke="#0b6e4f" stroke-width="4"/>
  <rect x="0" y="0" width="600" height="56" fill="#0b6e4f"/>
  <text x="24" y="36" font-family="Helvetica" font-size="22" font-weight="bold" fill="#ffffff">{{title}}</text>
  <text x="24" y="78" font-family="Helvetica" font-size="12" fill="#555555">Issued by {{issuerName}}</text>

  <rect x="430" y="92" width="146" height="168" fill="#f2f2f2" stroke="#cccccc" stroke-width="1"/>
  <image x="430" y="92" width="146" height="168" href="{{photo}}" preserveAspectRatio="xMidYMid slice"/>

  <text x="24" y="120" font-family="Helvetica" font-size="12" font-weight="bold" fill="#0b6e4f">FULL NAME</text>
  <text x="24" y="140" font-family="Helvetica" font-size="14">{{fullName}}</text>
  <text x="24" y="172" font-family="Helvetica" font-size="12" font-weight="bold" fill="#0b6e4f">FARMER ID</text>
  <text x="24" y="192" font-family="Helvetica" font-size="14">{{functionalRecordId}}</text>
  <text x="24" y="224" font-family="Helvetica" font-size="12" font-weight="bold" fill="#0b6e4f">DATE OF BIRTH</text>
  <text x="24" y="244" font-family="Helvetica" font-size="14">{{dateOfBirth}}</text>
  <text x="240" y="224" font-family="Helvetica" font-size="12" font-weight="bold" fill="#0b6e4f">GENDER</text>
  <text x="240" y="244" font-family="Helvetica" font-size="14">{{gender}}</text>

  <image x="24" y="256" width="116" height="116" href="{{qr}}"/>
  <text x="150" y="372" font-family="Helvetica" font-size="9" fill="#777777">Scan the QR to verify this credential offline.</text>
</svg>
